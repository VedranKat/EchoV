# EchoV Tools

Do not commit llama.cpp runtime binaries here.

EchoV now installs or selects the llama.cpp runtime from the app's
post-processing settings. Managed runtimes are stored under Application Support
instead of being bundled from the repo.
